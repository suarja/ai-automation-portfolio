export const metadata = {
  title: "Template CLAUDE.md pour Développeurs",
  description: "Fichier CLAUDE.md complet et prêt à utiliser pour vos projets de développement",
  longDescription: "Un template CLAUDE.md professionnel, basé sur les meilleures pratiques de context engineering. Copiez-le dans votre projet et adaptez-le à vos besoins pour maximiser l'efficacité de Claude Code.",
  image: "/images/icons/folder.png",
  tags: ["IA", "Claude", "CLAUDE.md", "Développement"],
  price: "Gratuit",
  featured: true,
  coverImage: "/images/icons/folder.png",
  gallery: []
}

# Template CLAUDE.md pour Développeurs

Un fichier `CLAUDE.md` est la **source de vérité centrale** pour Claude Code. Il fournit le contexte, les standards et les règles qui guident l'IA dans votre projet.

## 🚀 Installation et Mise en Place

### Prérequis : Installer Claude Code (CLI)

Si vous n'avez pas encore installé Claude Code :

```bash
# Installation via npm (recommandé)
npm install -g @anthropics/claude-code

# Ou via Homebrew (macOS/Linux)
brew install anthropics/tap/claude-code

# Vérifiez l'installation
claude --version
```

---

## 🎯 Méthode Recommandée : Laissez Claude faire le travail !

**Au lieu de copier-coller manuellement le template, utilisez Claude Code pour générer un `CLAUDE.md` personnalisé.**

### Pourquoi c'est mieux ?

✅ **Automatique** : Claude détecte votre stack, versions, et architecture
✅ **Personnalisé** : Contenu adapté à VOTRE projet, pas un template générique
✅ **Rapide** : 2 minutes vs 30 minutes de copier-coller manuel
✅ **Précis** : Aucun oubli, aucune erreur de version

### Comment faire ?

1. Copiez le template ci-dessous dans `~/Documents/Templates/CLAUDE-TEMPLATE.md`
2. Dans votre projet, lancez `claude`
3. Tapez `/init` et référencez le template avec `@CLAUDE-TEMPLATE.md`
4. Claude analyse votre projet ET le template pour créer un `CLAUDE.md` parfait

**Détails par scénario ci-dessous** ⬇️

---

## 📍 Approches selon l'état de votre projet

### Approche 1 : Nouveau projet (dossier vide ou presque)

**Situation :** Vous démarrez un nouveau projet et voulez configurer CLAUDE.md dès le début.

**Étape 1 : Préparez le template**

Téléchargez ou copiez le template de cette page dans un dossier de référence :

```bash
# Créez un dossier pour vos templates
mkdir -p ~/Documents/Templates

# Copiez le template (utilisez le bouton Copier sur le bloc de code ci-dessous)
# Créez le fichier CLAUDE-TEMPLATE.md et collez-y le contenu
```

**Étape 2 : Utilisez Claude pour générer votre CLAUDE.md**

```bash
# Naviguez vers votre nouveau projet
cd /chemin/vers/nouveau-projet

# Lancez Claude et utilisez /init avec référence au template
claude
```

Dans Claude Code, tapez :

```
/init

Crée un fichier CLAUDE.md pour ce projet en t'inspirant du template
@~/Documents/Templates/CLAUDE-TEMPLATE.md

Analyse le projet (même s'il est vide) et génère un CLAUDE.md adapté avec :
- La stack que tu détectes (package.json, fichiers config)
- Les outils de build présents
- Une structure adaptée à ce type de projet

Utilise le template comme référence pour la structure, mais personnalise
tout le contenu selon ce projet spécifique.
```

**Résultat :** Claude va :
1. ✅ Lire votre template comme référence
2. ✅ Analyser votre projet (dépendances, fichiers config, structure)
3. ✅ Générer un `CLAUDE.md` personnalisé qui combine le template et votre projet

---

### Approche 2 : Projet déjà commencé (code existant)

**Situation :** Vous avez déjà du code et voulez ajouter un CLAUDE.md adapté.

**Étape 1 : Sauvegardez le template**

```bash
# Même chose, stockez le template quelque part
mkdir -p ~/Documents/Templates
# Copiez le template dans CLAUDE-TEMPLATE.md
```

**Étape 2 : Laissez Claude analyser et créer**

```bash
# Depuis la racine de votre projet
cd /chemin/vers/projet-existant

# Lancez Claude
claude
```

Dans Claude Code :

```
/init

Analyse ce projet en profondeur et crée un fichier CLAUDE.md complet.

Inspire-toi de la structure de @~/Documents/Templates/CLAUDE-TEMPLATE.md

Mais remplis-le avec les informations réelles de CE projet :
- Détecte la stack (regarde package.json, imports, fichiers config)
- Analyse l'architecture actuelle (dossiers, patterns utilisés)
- Identifie les conventions de code déjà en place
- Détecte les outils de test, build, etc.
- Propose des "pièges communs" basés sur la stack détectée

Je veux un CLAUDE.md qui reflète EXACTEMENT ce projet, pas un template générique.
```

**Résultat :** Claude va :
1. ✅ Scanner votre codebase
2. ✅ Détecter votre stack et architecture
3. ✅ Utiliser le template pour la structure
4. ✅ Générer un CLAUDE.md qui correspond à votre projet réel

**Avantages :**
- Pas besoin de remplir manuellement les placeholders
- Claude détecte automatiquement votre stack
- Le CLAUDE.md est immédiatement pertinent

---

### Approche 3 : Améliorer un CLAUDE.md existant

**Situation :** Vous avez déjà un CLAUDE.md basique et voulez l'enrichir.

```bash
# Depuis votre projet
claude
```

Dans Claude Code :

```
J'ai déjà un fichier CLAUDE.md mais il est incomplet.

Inspire-toi de la structure complète de @~/Documents/Templates/CLAUDE-TEMPLATE.md
et enrichis mon CLAUDE.md actuel avec :

- Sections manquantes (ADRs, pièges communs, workflows, etc.)
- Améliore les sections existantes avec plus de détails
- Garde tout le contenu déjà présent qui est pertinent
- Ajoute des exemples concrets basés sur le code du projet

Fais une mise à jour intelligente, pas une réécriture complète.
```

**Résultat :** Claude va :
1. ✅ Lire votre CLAUDE.md actuel
2. ✅ Comparer avec le template de référence
3. ✅ Ajouter les sections manquantes
4. ✅ Enrichir les sections existantes

---

### Approche 4 : Manuelle (si vous préférez le contrôle total)

**Pour les puristes qui veulent tout faire à la main :**

```bash
# Copiez le template directement dans votre projet
cp ~/Documents/Templates/CLAUDE-TEMPLATE.md ./CLAUDE.md

# Éditez manuellement et remplacez tous les [PLACEHOLDERS]
code CLAUDE.md  # ou vim, nano, etc.
```

Remplacez manuellement :
- `[NOM_DU_PROJET]` → Nom de votre projet
- `[Framework + version]` → Votre stack
- `[Database]` → Votre base de données
- Etc.

---

## ✅ Vérification : Testez votre CLAUDE.md

Quelle que soit l'approche choisie, testez que Claude comprend bien votre projet :

```bash
# Démarrez Claude
claude

# Testez la compréhension
claude "Résume l'architecture de ce projet en 3 bullets"
claude "Quels sont les standards de code à respecter ?"
claude "Comment créer un nouveau service avec tests dans ce projet ?"
```

**Si les réponses sont précises et pertinentes** → ✅ Votre CLAUDE.md fonctionne !

**Si Claude est vague ou générique** → Améliorez votre CLAUDE.md avec plus de détails spécifiques.

---

## 🔄 Committez dans Git

Une fois satisfait :

```bash
git add CLAUDE.md
git commit -m "docs: add comprehensive CLAUDE.md for AI context engineering"
git push
```

## 📋 Qu'est-ce qu'un fichier CLAUDE.md ?

- **Emplacement** : Racine de votre projet
- **Format** : Markdown simple
- **Chargement** : Automatique par Claude Code au démarrage
- **Contenu** : Architecture, standards, commandes, règles, décisions
- **Stack-agnostic** : Adaptable à React, Vue, Node.js, Python, Go, etc.
- **Évolutif** : Mettez-le à jour au fur et à mesure de l'évolution du projet

## 💡 Pourquoi laisser Claude générer plutôt que copier-coller ?

### ✅ Avantages de l'approche "Claude génère"

**1. Détection automatique de la stack**
```bash
# Claude analyse automatiquement :
- package.json → détecte React 18, TypeScript, Vite
- tsconfig.json → voit vos configurations TypeScript
- vitest.config.ts → comprend votre setup de tests
- Structure de dossiers → identifie vos patterns
```

**2. Contenu personnalisé, pas générique**
```
Template générique : "[Framework + version]"
Claude génère : "React 18.2 avec Vite 5.1 et TypeScript 5.3"
```

**3. Gain de temps énorme**
- Pas de recherche manuelle de versions
- Pas de copier-coller de configurations
- Pas d'oubli de sections importantes

**4. S'adapte à l'existant**
```bash
# Pour un projet déjà commencé, Claude détecte :
- Les conventions de nommage déjà en place
- Les patterns architecturaux utilisés
- Les outils de build/test/lint configurés
- Les dépendances importantes
```

### Exemple concret : Nouveau projet React

**Sans Claude (manuel) :**
```bash
# 1. Copier le template
# 2. Chercher la version de React dans package.json
# 3. Chercher la version de TypeScript
# 4. Chercher la version de Vite
# 5. Lister manuellement toutes les dépendances importantes
# 6. Deviner l'architecture (aucun code encore)
# ⏱️ Temps : ~30 minutes
```

**Avec Claude :**
```bash
claude
> /init
>
> Crée CLAUDE.md en t'inspirant de @~/Documents/Templates/CLAUDE-TEMPLATE.md
> Analyse package.json et génère un fichier complet et personnalisé.

# ⏱️ Temps : ~2 minutes
# ✅ Plus précis et complet
```

---

## 💡 Commandes Claude utiles

Une fois votre CLAUDE.md en place :

```bash
# Démarrer une session interactive
claude

# Poser une question directe sur le projet
claude "Comment tester ce service ?"

# Générer du code conforme à vos standards
claude "Crée un nouveau composant Button avec tests Vitest"

# Analyser du code existant
claude "Explique-moi ce fichier: src/services/user-service.ts"

# Refactorer selon vos conventions
claude "Refactor ce composant pour suivre nos standards"

# Utiliser temporairement un contexte différent
claude --context ~/Templates/ALTERNATIVE-CLAUDE.md "Analyse ce code"
```

---

## Fichier CLAUDE.md complet

```markdown
# CLAUDE.md

*Dernière mise à jour : [DATE]*

## Aperçu du Projet

**Nom :** [NOM_DU_PROJET]
**Type :** [Web App / API / CLI / Library / Mobile]
**Stack :** [React/Vue/Svelte] + [TypeScript/JavaScript] + [Framework backend]
**Objectif :** [Description en 1-2 phrases du but du projet]

### Technologies Principales

- **Frontend :** [Framework + version] (ex: React 18, Vue 3, Svelte 4)
- **Backend :** [Framework + version] (ex: Express 4, Fastify 4, Next.js API Routes)
- **Database :** [Type + version] (ex: PostgreSQL 15, MongoDB 6)
- **ORM/Query :** [Tool] (ex: Prisma 5, Drizzle, TypeORM)
- **Styling :** [Approach] (ex: Tailwind CSS 3, CSS Modules, Styled Components)
- **Testing :** Vitest 1.x (unit/integration) + [E2E tool] (Playwright/Cypress)
- **Build Tool :** [Vite 5 / Webpack 5 / Turbopack / esbuild]
- **Package Manager :** [npm / pnpm / yarn / bun]
- **State Management :** [Zustand / Redux / Jotai / Context] (si applicable)
- **Validation :** Zod 3.x

## Architecture

### Context Engineering Principles

1. **Context is King** - Toujours travailler avec le contexte approprié
2. **Type-First Development** - Types avant implémentation
3. **Change Tracking** - Documenter chaque décision architecturale
4. **Systematic Testing** - Tests automatiques avec Vitest

### Folder Structure

\`\`\`
project-root/
├── src/
│   ├── components/        # UI components
│   ├── services/          # Business logic (stack agnostic)
│   ├── lib/
│   │   ├── db/            # Database layer
│   │   ├── types/         # TypeScript types
│   │   └── utils/         # Pure utility functions
│   ├── hooks/             # Custom hooks (si React/Vue)
│   └── __tests__/         # Test files
│       ├── unit/
│       ├── integration/
│       └── e2e/
├── docs/
│   ├── decisions/         # ADRs (Architectural Decision Records)
│   └── README.md          # Project documentation
└── [config files]
\`\`\`

### Architectural Principles

**Separation of Concerns**
- UI layer (`components/`) ne contient QUE de la présentation
- Business logic isolée dans `services/`
- Data access centralisé dans `lib/db/`

**Dependency Direction**
\`\`\`
UI → Services → Database
  ↓
Types (shared across all layers)
\`\`\`

## Standards de Code

### Naming Conventions

**Files & Folders**
- Components: `PascalCase.tsx` (ex: `UserProfile.tsx`)
- Services: `kebab-case.ts` (ex: `user-service.ts`)
- Utilities: `kebab-case.ts` (ex: `format-date.ts`)
- Types: `kebab-case.types.ts` (ex: `user.types.ts`)
- Tests: `*.spec.ts` (Vitest) ou `*.test.ts`

**Code**
- Functions: `camelCase` (ex: `getUserById`)
- Constants: `UPPER_SNAKE_CASE` (ex: `API_BASE_URL`)
- Types/Interfaces: `PascalCase` (ex: `User`, `ApiResponse`)
- Private functions: `_prefixWithUnderscore` (optionnel)

### Type System & Branded Types

**Utiliser des branded types pour le domain modeling :**

\`\`\`typescript
// lib/types/branded.ts
export type Brand<T, TBrand> = T & { __brand: TBrand };

// lib/types/domain.ts
export type UserId = Brand<string, 'UserId'>;
export type Email = Brand<string, 'Email'>;
export type Timestamp = Brand<number, 'Timestamp'>;

// Usage
interface User {
  id: UserId;
  email: Email;
  createdAt: Timestamp;
}
\`\`\`

**Pourquoi ?** Évite les erreurs subtiles (passer un `ProductId` à une fonction attendant `UserId`).

### Validation at Boundaries

**Toujours valider avec Zod aux points d'entrée :**

\`\`\`typescript
import { z } from 'zod';

const UserSchema = z.object({
  email: z.string().email(),
  name: z.string().min(2),
  age: z.number().int().positive(),
});

// API route / form handler
const validated = UserSchema.parse(input); // Throws si invalide
\`\`\`

### Error Handling Pattern

\`\`\`typescript
// Custom errors pour le domaine métier
export class UserNotFoundError extends Error {
  constructor(userId: UserId) {
    super(\`User \${userId} not found\`);
    this.name = 'UserNotFoundError';
  }
}

// Service layer
export async function getUserById(id: UserId): Promise<User> {
  const user = await db.query(/* ... */);
  if (!user) throw new UserNotFoundError(id);
  return user;
}

// API layer - transform errors
try {
  const user = await getUserById(userId);
  return { success: true, data: user };
} catch (error) {
  if (error instanceof UserNotFoundError) {
    return { success: false, error: 'User not found', status: 404 };
  }
  return { success: false, error: 'Internal error', status: 500 };
}
\`\`\`

## Commandes Essentielles

### Development
\`\`\`bash
[npm/pnpm/yarn] run dev          # Start dev server
[npm/pnpm/yarn] run build        # Production build
[npm/pnpm/yarn] run preview      # Preview production build
[npm/pnpm/yarn] run lint         # Run ESLint
[npm/pnpm/yarn] run type-check   # TypeScript compilation check
\`\`\`

### Testing (Vitest)
\`\`\`bash
[npm/pnpm/yarn] test              # Run all tests
[npm/pnpm/yarn] test:unit         # Unit tests only
[npm/pnpm/yarn] test:integration  # Integration tests
[npm/pnpm/yarn] test:e2e          # E2E tests
[npm/pnpm/yarn] test:watch        # Watch mode
[npm/pnpm/yarn] test:coverage     # Coverage report (target: >80%)
\`\`\`

### Database (si applicable)
\`\`\`bash
# Adapter selon votre ORM (Prisma exemple ci-dessous)
npx prisma migrate dev            # Create & apply migration
npx prisma generate               # Generate client
npx prisma studio                 # Database GUI
\`\`\`

### Git Workflow
\`\`\`bash
git checkout -b feature/[name]    # Create feature branch
# Make changes...
[npm/pnpm/yarn] run pre-commit    # Lint + Type-check + Test
git commit -m "type(scope): desc" # Conventional Commits
git push origin feature/[name]
# Create Pull Request
\`\`\`

## Workflows

### Feature Development (TDD Approach)

1. **Write failing test**
\`\`\`typescript
// services/user-service.spec.ts
import { describe, it, expect } from 'vitest';
import { createUser } from './user-service';

describe('createUser', () => {
  it('should create user with valid email', async () => {
    const user = await createUser({ email: 'test@example.com' });
    expect(user.email).toBe('test@example.com');
  });
});
\`\`\`

2. **Implement minimum code to pass**
3. **Refactor while keeping tests green**
4. **Document architectural decisions if needed**

### Testing Strategy

**Unit Tests** - Pure functions, isolated logic
\`\`\`typescript
// lib/utils/slugify.spec.ts
import { describe, it, expect } from 'vitest';
import { slugify } from './slugify';

describe('slugify', () => {
  it('converts string to URL-safe slug', () => {
    expect(slugify('Hello World!')).toBe('hello-world');
  });
});
\`\`\`

**Integration Tests** - Services with dependencies
\`\`\`typescript
// services/user-service.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { createUser, getUserById } from './user-service';

describe('UserService Integration', () => {
  beforeEach(async () => {
    await resetTestDatabase();
  });

  it('creates and retrieves user', async () => {
    const created = await createUser({ email: 'test@example.com' });
    const retrieved = await getUserById(created.id);
    expect(retrieved).toEqual(created);
  });
});
\`\`\`

## Pièges Communs & Solutions

### TypeScript Gotchas

❌ **Type assertion sans validation**
\`\`\`typescript
const user = data as User; // Unsafe!
\`\`\`

✅ **Runtime validation avec Zod**
\`\`\`typescript
const user = UserSchema.parse(data); // Type + validation
\`\`\`

### Async/Await Anti-patterns

❌ **Await dans boucles (N+1 problem)**
\`\`\`typescript
for (const id of userIds) {
  const user = await getUser(id); // Sequential, slow
}
\`\`\`

✅ **Promise.all pour parallélisme**
\`\`\`typescript
const users = await Promise.all(
  userIds.map(id => getUser(id))
);
\`\`\`

### Database Query Patterns

❌ **N+1 queries**
\`\`\`typescript
const users = await db.user.findMany();
for (const user of users) {
  user.posts = await db.post.findMany({ where: { userId: user.id } });
}
\`\`\`

✅ **Eager loading**
\`\`\`typescript
const users = await db.user.findMany({
  include: { posts: true }
});
\`\`\`

## Règles de Comportement

### Ce que tu dois faire

✅ **Type Safety First**
- Définis les types AVANT l'implémentation
- Utilise des branded types pour les domain models
- Valide avec Zod aux boundaries (API, forms, external data)

✅ **Test Coverage**
- Propose des tests unitaires pour les fonctions utilitaires
- Crée des tests d'intégration pour les services avec dépendances
- Suis le pattern `describe` > `it` avec Vitest
- Vise >80% coverage sur le code critique

✅ **Documentation Inline**
- Ajoute JSDoc pour les fonctions publiques
- Commente les décisions non-évidentes
- Mets à jour CLAUDE.md si changement architectural

✅ **Error Handling Robuste**
- Ajoute try-catch aux points d'entrée (API, event handlers)
- Crée des custom errors pour les cas métier
- Utilise un logging approprié (structured logging en production)

### Ce que tu ne dois PAS faire

❌ **Ignorer la validation**
- N'accepte JAMAIS d'input utilisateur sans validation Zod
- N'utilise pas `any` sans justification explicite

❌ **Créer des dépendances circulaires**
- Vérifie les imports avant d'ajouter du code
- Les services ne doivent pas importer de UI components

❌ **Patterns obsolètes ou non-idiomatiques**
- Évite les callbacks quand async/await est possible
- N'utilise pas `var`, utilise toujours `const`/`let`
- N'utilise pas `@ts-ignore` sans commentaire explicatif

❌ **Tests insuffisants**
- N'implémente pas de logique métier sans tests
- Ne modifie pas de code critique sans vérifier les tests

## Décisions Architecturales (ADRs)

### ADR-001: Branded Types pour Type Safety

**Date:** [YYYY-MM-DD]
**Status:** ✅ Accepted

**Context:**
TypeScript permet confusion entre types primitifs similaires (userId vs productId tous deux `string`).

**Decision:**
Utiliser branded types pour tous identifiants et valeurs métier.

**Consequences:**
- ✅ Erreurs de type détectées à la compilation
- ✅ Documentation self-evident du domaine
- ⚠️ Légère verbosité, nécessite helpers

### ADR-002: Vitest pour Testing

**Date:** [YYYY-MM-DD]
**Status:** ✅ Accepted

**Context:**
Besoin d'un test runner rapide avec excellent support TypeScript.

**Decision:**
Vitest comme framework de test principal.

**Consequences:**
- ✅ 10-20x plus rapide que Jest
- ✅ ESM natif, config minimale
- ✅ API compatible Jest (migration facile)
- ⚠️ Ecosystem plus récent

### ADR-003: Zod pour Validation Runtime

**Date:** [YYYY-MM-DD]
**Status:** ✅ Accepted

**Context:**
TypeScript types disparaissent à runtime. Besoin validation des inputs externes.

**Decision:**
Zod pour validation + inference de types.

**Consequences:**
- ✅ Single source of truth (schema → types)
- ✅ Messages d'erreur clairs
- ✅ Validation composable
- ⚠️ Bundle size légèrement plus gros

## Debugging Quick Reference

**Build fails mais dev fonctionne**
\`\`\`bash
# TypeScript errors probables
npm run type-check
# Vérifier les imports, any types, etc.
\`\`\`

**Tests échouent de manière aléatoire**
\`\`\`bash
# Souvent des tests non-isolés
# Vérifier beforeEach/afterEach dans les specs
# Regarder les mocks/stubs non nettoyés
\`\`\`

**Performance dégradée**
\`\`\`bash
# Profiler avec dev tools
# Vérifier N+1 queries (database)
# Bundle size analysis
npm run build -- --analyze
\`\`\`

## Variables d'Environnement

\`\`\`bash
# .env.example (committer celui-ci)
DATABASE_URL="[connection_string]"
API_KEY="[your_api_key]"
NODE_ENV="development"

# .env.local (ne PAS committer)
# Copier .env.example et remplir avec vraies valeurs
\`\`\`

**⚠️ Sécurité:** Ne JAMAIS committer `.env.local`. Toujours utiliser `.env.example` pour documentation.

## Ressources

**Documentation**
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Vitest Docs](https://vitest.dev)
- [Zod Docs](https://zod.dev)

**Outils Internes**
- Test Coverage: `npm run test:coverage` → `coverage/index.html`
- Type Coverage: `npx type-coverage --detail`

**Style Guide**
- Conventional Commits: https://www.conventionalcommits.org/
- Semantic Versioning: https://semver.org/

---

*Maintenu par : [ÉQUIPE/PERSONNE]*
*Questions : [EMAIL/SLACK]*
```

---

## Variantes du template

### Mini CLAUDE.md (Projets simples)

Pour prototypes ou projets personnels, utilisez cette version allégée :

```markdown
# CLAUDE.md

## Stack
[Framework], TypeScript, [CSS approach], [Database]

## Structure
- `src/` - Source code
- `src/__tests__/` - Tests (Vitest)

## Commandes
\`\`\`bash
npm run dev   # Dev server
npm test      # Run tests
npm run build # Production build
\`\`\`

## Standards
- Files: kebab-case
- Types: PascalCase
- Always validate inputs with Zod
- >80% test coverage on critical paths

## Pièges
- [Piège spécifique à votre stack]
- [Common gotcha #2]
```

---

## 🎯 Pourquoi un fichier CLAUDE.md est essentiel

### Les bénéfices d'un bon CLAUDE.md

✅ **Cohérence** : Toute l'équipe (humains + IA) suit les mêmes standards
✅ **Productivité** : Claude génère du code conforme dès le premier coup
✅ **Onboarding** : Nouveaux développeurs ET l'IA comprennent le projet rapidement
✅ **Documentation vivante** : Se met à jour avec le projet, contrairement aux docs statiques
✅ **Réduction d'erreurs** : Claude connaît les pièges spécifiques à votre stack

### Cas d'usage concrets

**Génération de code :**
```bash
claude "Crée un service User avec validation Zod et tests Vitest"
# → Code généré suivant EXACTEMENT vos standards
```

**Refactoring :**
```bash
claude "Refactor ce composant selon nos conventions"
# → Respecte votre architecture et naming conventions
```

**Debug et résolution de problèmes :**
```bash
claude "Ce test échoue, aide-moi à débugger"
# → Claude connaît votre setup de tests et peut pointer les problèmes
```

## 📚 Guide d'utilisation avancé

### Évolution et maintenance du CLAUDE.md

Traitez votre `CLAUDE.md` comme du code source :

**1. Versionnez chaque changement**
```bash
git commit -m "docs(claude): add API error handling patterns"
```

**2. Ajoutez des pièges au fur et à mesure**

Chaque fois que vous découvrez un piège ou une erreur récurrente :

```markdown
## Pièges Communs

### Nouvelle entrée découverte
❌ **[Description du problème]**
✅ **[Solution]**
```

**3. Documentez les décisions architecturales (ADRs)**

Utilisez la section ADR pour tracer vos décisions importantes :

```markdown
### ADR-004: Passage de Redux à Zustand

**Date:** 2024-12-15
**Status:** ✅ Accepted

**Context:** Redux générait trop de boilerplate pour notre équipe

**Decision:** Migration vers Zustand pour state management

**Consequences:**
- ✅ Moins de code, plus de productivité
- ✅ TypeScript support excellent
- ⚠️ Migration des tests nécessaire
```

### Multi-environnements et configurations personnelles

Claude Code supporte une hiérarchie de fichiers :

**1. Global (`~/.claude/CLAUDE.md`)**
```markdown
# Mes préférences personnelles globales
- Style d'écriture préféré
- Patterns que j'utilise systématiquement
```

**2. Projet (`./CLAUDE.md`)**
```markdown
# Standards de l'équipe (committé dans Git)
- Architecture du projet
- Conventions de code
- Décisions techniques
```

**3. Local (`./CLAUDE.local.md` - gitignored)**
```markdown
# Mes préférences locales qui overrident le fichier projet
- Raccourcis personnels
- Notes de développement
```

Claude charge et fusionne ces fichiers dans cet ordre de priorité :
`Global` < `Projet` < `Local`

### Monorepos et projets complexes

Pour les monorepos, créez une structure hiérarchique :

```
monorepo/
├── CLAUDE.md                    # Context global
├── packages/
│   ├── frontend/
│   │   └── CLAUDE.md           # Context frontend
│   ├── backend/
│   │   └── CLAUDE.md           # Context backend
│   └── shared/
│       └── CLAUDE.md           # Context partagé
```

Dans le `CLAUDE.md` racine, référencez les autres :

```markdown
# CLAUDE.md (racine du monorepo)

## Structure du Monorepo

Ce projet est un monorepo avec plusieurs packages :

- `packages/frontend/` - Application React (voir @packages/frontend/CLAUDE.md)
- `packages/backend/` - API Node.js (voir @packages/backend/CLAUDE.md)
- `packages/shared/` - Types et utils partagés (voir @packages/shared/CLAUDE.md)

## Standards Globaux

[Standards qui s'appliquent à tous les packages]
```

---

## 🔧 Conseils de Pro

### Taille optimale d'un CLAUDE.md

- **Minimum viable** : ~500 lignes (stack, architecture, standards de base)
- **Idéal pour projet moyen** : 1000-1500 lignes
- **Grand projet / monorepo** : Split en plusieurs fichiers (voir section Monorepos)

**Règle d'or** : Si Claude pose trop de questions → ajoutez plus de contexte. Si Claude est confus → simplifiez et structurez mieux.

### Testez votre CLAUDE.md régulièrement

Créez un "test suite" de prompts standards :

```bash
# Testez la compréhension de l'architecture
claude "Résume l'architecture en 3 bullets"

# Testez la génération de code
claude "Crée un nouveau service UserProfile avec validation et tests"

# Testez la connaissance des pièges
claude "Quels sont les 3 pièges majeurs de notre stack ?"
```

Si les réponses ne sont pas satisfaisantes → améliorez votre CLAUDE.md.

### Partagez avec votre équipe

Encouragez toute l'équipe à contribuer :

```bash
# Créez un template de PR pour améliorer CLAUDE.md
.github/PULL_REQUEST_TEMPLATE/claude_md_update.md
```

```markdown
## Modification du CLAUDE.md

**Type de changement :**
- [ ] Nouveau piège découvert
- [ ] Nouvelle convention de code
- [ ] Décision architecturale (ADR)
- [ ] Mise à jour de dépendances
- [ ] Autre : _______

**Description :**
[Décrivez ce qui change et pourquoi]

**Impact :**
[Comment cela affecte le développement quotidien ?]
```

---

## 📖 Ressources complémentaires

### Documentation officielle
- **Guide officiel Claude Code** : [docs.anthropic.com/claude-code](https://docs.anthropic.com/claude-code)
- **CLAUDE.md best practices** : [stevekinney.com - CLAUDE.md](https://stevekinney.com/courses/ai-development/claude-dot-md)
- **Context Engineering** : Principes avancés de context engineering

### Exemples et inspiration
- **Exemples GitHub** : [Rechercher filename:CLAUDE.md](https://github.com/search?q=filename%3ACLAUDE.md)
- **Templates communautaires** : Explorez les repositories open-source pour voir des CLAUDE.md réels

### Aller plus loin
- **Custom Prompts** : Créez des alias personnalisés pour des tâches récurrentes
- **CI/CD Integration** : Intégrez Claude dans vos pipelines pour review automatique
- **Team Workflows** : Établissez des conventions d'équipe pour maintenir CLAUDE.md

---

## 💬 Besoin d'aide ?

**Questions fréquentes :**

**Q : Mon CLAUDE.md est trop long, Claude ne le lit pas en entier ?**
R : Claude Code charge tout le fichier. Si trop verbeux, split en plusieurs fichiers ou utilisez des monorepos patterns.

**Q : Dois-je tout mettre dans CLAUDE.md ?**
R : Non. Mettez l'essentiel : architecture, standards, pièges. Pas besoin de documenter chaque fonction.

**Q : Mon équipe n'utilise pas Claude, est-ce quand même utile ?**
R : Oui ! CLAUDE.md sert aussi de documentation vivante pour les humains. C'est un excellent README technique.

**Q : À quelle fréquence mettre à jour ?**
R : Mettez à jour à chaque décision architecturale majeure ou nouveau piège découvert. Minimum : revue mensuelle.

---

## 🎁 Template téléchargeable

Vous voulez un fichier à copier directement ?

**Téléchargez le template :**
1. Copiez le bloc de code "Fichier CLAUDE.md complet" ci-dessus
2. Créez un fichier `CLAUDE-TEMPLATE.md` sur votre machine
3. Personnalisez-le pour votre projet
4. Renommez-le en `CLAUDE.md` et placez-le à la racine de votre projet

**Besoin d'aide pour adapter le template à votre stack ?**

👉 [Réservez un appel de 30 minutes](https://cal.com/jasonsuarez/booking) et je vous aiderai à créer un CLAUDE.md parfait pour votre projet.

---

*Template créé par Jason Suarez | [media.jason-suarez.com](https://media.jason-suarez.com)*
*Inspiré des meilleures pratiques de context engineering et des retours de la communauté Claude*
